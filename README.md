Sources for the plugin `plugin.tapticEngine`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.tapticEngine" = {
            publisherId = "tech.scotth",
        },
    },
}
```
